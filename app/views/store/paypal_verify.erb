<% if @last_order != nil %>
  Click <%= link_to "here", customer_order_path(
  {:customer_id => @last_order.customer_id, :id => @last_order.id}) %>
  to download your purchase.<br/><br/>
<%  end %>

<%= @msg %>